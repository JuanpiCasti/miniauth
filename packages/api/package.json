{
  "name": "@miniauth/api",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts --inspect",
    "lint": "eslint .",
    "format": "prettier . --write",
    "build": "bun build --minify-whitespace --minify-syntax --target bun --outfile ./dist/index.js src/index.ts"
  },
  "dependencies": {
    "@elysiajs/openapi": "^1.4.13",
    "@miniauth/schemas": "workspace:*",
    "elysia": "latest",
    "jose": "^6.1.3"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
